'''
if
'''
'''
if
    .lower()------临时转换为小写
=,==,!=,>,<,<=,>=
and,or
in,not in (检查是否在列表中)
if-else,if-elif-else,语句
    如果要测试多个条件为true的情况，则就要使用多个if语句才可以
    检查特殊元素------添加for循环
    确定列表非空------if A：  else    即可,为空时返回false，即进入else
    使用多个列表
    
代码格式
    在==,<=,>=等比较符号的左右各添加一个空格
    虽然没有说，但是在赋值的 = 上也是留了空格的
这个三个引号的符号在处于读取的第一行时，就会是斜体，不是的时候就是正常的亮的字体
'''
